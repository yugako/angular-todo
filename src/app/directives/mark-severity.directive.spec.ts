import { MarkSeverityDirective } from './mark-severity.directive';

describe('MarkSeverityDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkSeverityDirective();
    expect(directive).toBeTruthy();
  });
});
